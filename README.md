# Rabobank Mortgage Portal - Playwright E2E Tests

## Overview

This repository contains automated end-to-end tests for the Rabobank Mortgage Application Portal using Playwright.

## Related Jira Story

- **User Story**: [RBTES-1130](https://parasutest.atlassian.net/browse/RBTES-1130) - Submit Mortgage Application - Rabobank Mortgage Portal

## Test Cases (Zephyr Scale)

| Test Case ID | Name | Priority | Acceptance Criteria |
|--------------|------|----------|---------------------|
| RBTES-T1615 | TC-F001: Verify successful mortgage application submission | High | AC-1.1 |
| RBTES-T1613 | TC-F002: Verify pre-filled customer data from profile | High | AC-1.2 |
| RBTES-T1614 | TC-F003: Verify mortgage calculation preview functionality | High | AC-1.3 |
| RBTES-T1612 | TC-F004: Verify validation of required fields | High | AC-1.4 |
| RBTES-T1611 | TC-F005: Verify dynamic income fields based on employment type | Normal | AC-1.5 |
| RBTES-T1616 | TC-E001: Verify LTV ratio warning for excessive mortgage amount | Normal | Edge Case |
| RBTES-T1610 | TC-E002: Verify handling of property outside Netherlands | Normal | Edge Case |
| RBTES-T1609 | TC-E003: Verify age eligibility restriction for under 18 | Normal | Edge Case |

## Prerequisites

- Node.js 18+
- npm or yarn

## Installation

```bash
npm install
npx playwright install
```

## Running Tests

```bash
# Run all tests
npm test

# Run tests with UI
npm run test:headed

# Run specific browser tests
npm run test:chromium
npm run test:firefox
npm run test:webkit

# Run mobile tests
npm run test:mobile

# Debug mode
npm run test:debug

# View test report
npm run test:report
```

## Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `MORTGAGE_PORTAL_URL` | Base URL of the mortgage portal | `https://hypotheek.rabobank.nl` |
| `TEST_USERNAME` | Test customer username | `test.customer@rabobank.nl` |
| `TEST_PASSWORD` | Test customer password | `TestPassword123!` |

## Project Structure

```
rabobank-mortgage-tests/
├── tests/
│   └── mortgage-application.spec.ts    # Main test file
├── playwright.config.ts                 # Playwright configuration
├── package.json                         # Dependencies
└── README.md                            # This file
```

## CI/CD Integration

Tests are configured to output reports in multiple formats:
- HTML report (`playwright-report/`)
- JSON results (`test-results/results.json`)
- JUnit XML (`test-results/junit.xml`)

## Browser Coverage

Tests run on:
- Chrome (Desktop)
- Firefox (Desktop)
- Safari/WebKit (Desktop)
- Edge (Desktop)
- Chrome (Mobile - Pixel 5)
- Safari (Mobile - iPhone 12)

## Non-Functional Tests

- **Accessibility**: WCAG 2.1 AA compliance checks
- **Performance**: Page load and form submission < 3 seconds

## Traceability

All test cases are linked to the Jira story RBTES-1130 in Zephyr Scale for full traceability.

---

*Generated by NAVI - 2026-05-29*
